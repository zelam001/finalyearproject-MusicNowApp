import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        margin: 7,
        borderRadius: 40,
        borderWidth: 3,
        borderColor: '#ede12f',


    },
    image: {
        borderRadius: 40,
        borderWidth: 3,
        borderColor: '#ffffff',
    }

});


export default styles;